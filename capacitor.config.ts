import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mariusschober.goalflow',
  appName: 'Goalflow',
  webDir: 'dist/client'
};

export default config;
